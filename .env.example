OPENAI_API_KEY=your_openai_api_key_here

# Duplicate this to a local .env and put your real key there.
# Never commit your real .env or any secret to git.

# For OpenRouter (300+ LLMs, free models available)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# For Grok (xAI's language models - Grok 2, Grok Vision)
GROK_API_KEY=your_grok_api_key_here
# Alternative name (both work):
# XAI_API_KEY=your_xai_api_key_here

# Lunch Money Budget Tracking
LUNCH_MONEY_API_KEY=your_lunchmoney_api_key_here

# Yahoo Finance (FREE - No API key required!)
# Uses yfinance Python library - unofficial but works great
# YFINANCE_ENABLED=true  # No API key needed!
# Provides: Real-time quotes, historical data, financials, dividends, analyst ratings
# Coverage: Global stocks, ETFs, mutual funds, currencies, crypto
# Limitations: Unofficial, no SLA, rate limits depend on Yahoo

# Rental Rates & ROI Analysis (FREE government data + optional paid API)
# Primary data source: HUD Fair Market Rents (FREE, no API key)
# Optional: RentCast API (50 free calls/month, then paid)
RENTCAST_API_KEY=your_rentcast_api_key_here_optional
# ROI Calculations: Cap rate, cash-on-cash return, GRM, cash flow analysis
# Also supports: Zillow CSV downloads, Redfin CSV downloads
# Perfect for: Real estate investment analysis, rental property evaluation

# API Helper Scripts:
# - scripts/api-helpers/openai_helper.py (GPT, DALL-E, Whisper, TTS, embeddings)
# - scripts/api-helpers/openrouter_helper.py (300+ models, free options)
# - scripts/api-helpers/grok_helper.py (Grok 2, Grok Vision, real-time info)
# - scripts/api-helpers/yfinance_helper.py (FREE stock market data, no API key)
# - scripts/api-helpers/rental_rates_helper.py (FREE rental data + ROI analysis)
# Claude Code will ask before using paid API services
